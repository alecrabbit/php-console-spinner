version: '3.7'
volumes:
  app:

services:
  &app-service app:
    image: php:8.1-alpine
    hostname: *app-service
    volumes:
      - ./:/app
    tty: true
    env_file:
      - ./.env
    ports:
      - "8080:8080"
    working_dir: /app
