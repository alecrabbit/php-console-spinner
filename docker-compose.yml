version: '3.7'

services:
  &service-app app:
    image: php:8.1-cli-alpine
    hostname: *service-app
    volumes:
      - ./:/app
    tty: true
    env_file:
      - ./.env
    ports:
      - "8080:8080"
    working_dir: /app
