deptrac:
  layers:
    - name: ModuleCore
      collectors:
        - type: bool
          must:
            - type: className
              value: .*AlecRabbit\\Spinner\\Core\\.*
          must_not:
            - type: className
              value: .*AlecRabbit\\Spinner\\Contract\\.*
