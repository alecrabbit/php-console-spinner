deptrac:
  layers:
    - name: ModuleCore
      collectors:
        - type: bool
          must:
            - type: className
              value: '%namespace%\\Core\\.*'
          must_not:
            - type: className
              value: '%namespace%\\Contract\\.*'
