parameters:
  ExtrasDirectory: src/Extras
deptrac:
  layers:
    - name: ModuleExtras
      collectors:
        - type: bool
          must:
            - type: directory
              regex: '%ExtrasDirectory%'
